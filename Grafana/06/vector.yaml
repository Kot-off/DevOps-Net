sources:
  docker_logs:
    type: docker_logs

sinks:
  elasticsearch_out:
    type: elasticsearch
    inputs:
      - docker_logs
    endpoints:
      - http://elasticsearch:9200
    mode: bulk
    compression: none
    healthcheck:
      enabled: false